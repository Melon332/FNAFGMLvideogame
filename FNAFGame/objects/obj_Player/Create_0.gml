cx = camera_get_view_x(view_camera[0]);

view_w = camera_get_view_width(view_camera[0]);
view_h = camera_get_view_height(view_camera[0]);

viewport_w = camera_get_view_x(view_camera[0]);

speed = 0;

camera_speed = 15;

boundless = false;

test = obj_room.sprite_width;



